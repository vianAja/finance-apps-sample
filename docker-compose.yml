services:
  app:
    build: .
    restart: unless-stopped
    network_mode: host
    environment:
      PORT: "3000"
      REDIS_HOST: 127.0.0.1
      REDIS_PORT: "6379"
      REDIS_USERNAME: admin
      REDIS_PASSWORD: Adinusa2026
      CACHE_TTL_SECONDS: "45"
      SESSION_TTL_SECONDS: "900"
      RATE_LIMIT_MAX: "5"
      RATE_LIMIT_WINDOW_SECONDS: "60"
